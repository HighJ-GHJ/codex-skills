# Handoff Entry Points

这个目录就是默认产物目录，方便在 Finder、资源管理器或普通文件浏览器里直接打开 handoff 文件。

运行 skill 后，这里会生成：

- `LATEST.md`
- `<handoff_id>.md`
- `<handoff_id>/`

其中 `<handoff_id>/` 就是真实 handoff 包本身，里面直接包含 `brief.md`、`bundle.md`、`manifest.json`、`reply_template.md`、`notes.md` 和 `preview.json`。
