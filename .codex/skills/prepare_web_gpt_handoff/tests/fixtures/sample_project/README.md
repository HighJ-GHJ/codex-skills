# Sample Project

This fixture project is used to test the prepare_web_gpt_handoff skill.
It contains a small set of documentation, source code, configuration, logs,
and excluded directories so the selection and truncation rules stay stable.
